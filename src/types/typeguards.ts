export function isAnchorElement(element: any): element is HTMLAnchorElement {
  return element instanceof HTMLAnchorElement;
}
export function isButtonElement(element: any): element is HTMLButtonElement {
  return element instanceof HTMLButtonElement;
}
